<%= form_for  [@post] do |f| %>
		<%= f.label :title %>
		<%= f.text_field :title %>
		<br>
		<%= f.label :gif_url %>
		<%= f.text_field :gif_url%>
		<br>
		<%= f.label :votes %>
		<%= f.number_field :votes %>
		<%= f.submit %>
<% end %>